# API base URL
# Dev: leave empty — Vite proxies /api/v1 to http://localhost:4000
# Prod: https://your-api.onrender.com/api/v1
VITE_API_BASE=

# Socket.io server (dev: backend origin; prod: same host as API without /api/v1 path)
# Example dev: http://localhost:4000
VITE_SOCKET_URL=
